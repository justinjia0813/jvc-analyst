# AI 时代的软件工程 → AI 时代的投资研究：系统性借鉴设计

> 基于《AI时代的软件工程》（鸟窝，2026）全书内容的跨领域迁移。
>
> 核心命题：**用结构化知识驾驭非结构化 AI 能力**——这个原则在软件工程和投资研究中同样成立。但两个领域在三个根本点上**不同构**，本文档用"设计零"专门处理这些边界。
>
> 最后更新：2026-07-30（v3：设计十二融入《投资研究决策图》数据架构，扩展为五层证据与主张图谱；决策日志增补活决策属性；路线图相应扩充。v2：新增设计零/十一/十二/十三，统一权威目录结构，成熟度模型改为 2×2）

---

## 目录

1. [总览：两条轨道的同构性](#1-总览两条轨道的同构性)
2. [设计零：类比的边界——不可迁移性分析](#2-设计零类比的边界不可迁移性分析)
3. [设计一：从 Prompt-Driven 到 Skill-Driven 的完整跃迁](#3-设计一从-prompt-driven-到-skill-driven-的完整跃迁)
4. [设计二：Spec-Driven Research——研究规格驱动](#4-设计二spec-driven-research研究规格驱动)
5. [设计三：Research Loop——自主循环研究引擎](#5-设计三research-loop自主循环研究引擎)
6. [设计四：虚拟投委会——多角色审查体系](#6-设计四虚拟投委会多角色审查体系)
7. [设计五：Compound Research——让每份研究都让下一份更容易](#7-设计五compound-research让每份研究都让下一份更容易)
8. [设计六：反合理化表——阻止 AI 跳过研究纪律](#8-设计六反合理化表阻止-ai-跳过研究纪律)
9. [设计七：Graph Research——并行化多轨研究编排](#9-设计七graph-research并行化多轨研究编排)
10. [设计八：Harness Engineering for Research——研究 Agent 的安全运行环境](#10-设计八harness-engineering-for-research研究-agent-的安全运行环境)
11. [设计九：上下文工程——对抗信息过载](#11-设计九上下文工程对抗信息过载)
12. [设计十：验证闭环——从"AI 说了"到"证据表明"](#12-设计十验证闭环从ai-说了到证据表明)
13. [设计十一：决策日志与结果回填——判断质量的复利](#13-设计十一决策日志与结果回填判断质量的复利)
14. [设计十二：证据与主张图谱——从文档到决策的数据架构](#14-设计十二证据与主张图谱从文档到决策的数据架构)
15. [设计十三：研究分级与流程密度——防弃用保险](#15-设计十三研究分级与流程密度防弃用保险)
16. [全局架构：投资研究的 AI 研发成熟度模型](#16-全局架构投资研究的-ai-研发成熟度模型)
17. [落地路线图](#17-落地路线图)
18. [附录 A：核心借鉴清单](#附录-a核心借鉴清单)
19. [附录 B：复用 vs 新建清单](#附录-b复用-vs-新建清单)

---

## 1. 总览：两条轨道的同构性

### 1.1 核心类比

《AI时代的软件工程》全书贯穿一个核心主张：**AI 不会救你，它会放大你。** 在 AI 让编码门槛降到历史最低点的时刻，工程化的价值反而达到了历史最高点。

投资研究领域面临完全同构的挑战：AI 可以让任何人快速生成一份"看起来不错"的投资分析，但让分析真正可信、可追溯、可积累、可验证——这是 AI 时代投资研究的**工程化问题**。

### 1.2 关键同构映射

| 软件工程概念 | 投资研究对应 | 说明 |
|-------------|-------------|------|
| 代码 | 研究报告/ memo | 最终交付物 |
| 测试 | 证据验证 | 每条论断需要可验证的证据支撑。但见设计零：投资研究没有客观的"测试通过"信号，验证必须分层 |
| 规格 (Spec) | 研究框架/尽调清单 | 定义"研究做完了"意味着什么 |
| 架构 | 投资逻辑框架 | 组织论据和推理的结构 |
| 重构 | 更新/修正分析 | 基于新信息更新原有判断 |
| 技术债 | 未验证假设 | 和技术债一样，未验证假设有"利息"：拖得越久验证成本越高，且它会被后续推理引用，利滚利 |
| CI/CD | 持续跟踪监控 | 投后管理和定期复盘。**本文档聚焦投前研究**，投后工程化另行展开（见设计零的范围声明） |
| Code Review | 交叉验证/同行审查 | 多人/多角度审视同一份分析 |
| Bug | 分析错误/逻辑漏洞 | 事实错误、推理跳跃、遗漏关键信息 |
| Release Review / Go-NoGo | 投决会 | 上线前的最终闸门：放行或拦停 |
| Deployment | 打款交割 | 资金进入，关系真正开始。此后的问题从"研究问题"变成"投后问题" |

### 1.3 四个层次的递进

书中提出的四层模型在投资研究中的映射：

```
软件工程                              投资研究
─────────────────────────────────────────────────────
Prompt Engineering  →  一次性提问（"帮我分析这个项目"）
Skill Engineering   →  可复用研究方法论（/jvc-prescreen 等）
Agent Orchestration →  多 Agent 协同研究（并行尽调、交叉验证）
Harness Engineering →  研究 Agent 的安全运行环境（权限、数据隔离、审计）
```

### 1.4 五个设计原则

来自书中第 1 章的核心主张，映射到投资研究：

1. **可复用** — 研究方法论写成 Skill，不同项目重复使用
2. **可验证** — 每条论断必须可追溯到证据源
3. **可迭代** — 研究方法本身随使用不断改进
4. **可组合** — 不同 Skill 可以拼接成完整研究流程
5. **不依赖特定模型** — Skill 用 Markdown 写，不绑定任何 AI 平台

---

## 2. 设计零：类比的边界——不可迁移性分析

> **类比是脚手架，不是蓝图。** 每一个从软件工程迁移过来的设计，都必须先回答一个问题："如果类比在这里失效，这个设计还成立吗？"

§1.2 的映射表画出了同构性的"阳面"。本节处理"阴面"——三个软件工程和投资研究**根本不同构**的点。它们不是细节差异，而是会改变设计形态的结构性差异。

### 2.1 三个根本不同构点

| 根本差异 | 软件工程 | 投资研究 | 对设计形态的影响 |
|---------|---------|---------|-----------------|
| **有无裁判（oracle）** | 测试通过/失败是客观信号，机器可判 | 没有测试套件。大部分"验证"是解释性的：来源互相冲突、证据需要解读。终极 ground truth（这笔投资赚不赚钱）5–10 年后才到达 | 验证不能类比测试，必须**分层**：事实验证（数字可核对）、推理验证（逻辑链可审查）、判断验证（只有时间能验证）。三层各用不同机制，见设计十 |
| **环境是否对抗** | 编译器是中立的，不会故意误导你 | 信息源有动机：创始人在 pitch、竞品在放烟雾弹、卖方在挑数据、中介在撮合 | "证据"本身是战略行为的产物。这更接近**安全工程**而非软件工程。每条证据必须分析其生成机制：谁提供的、动机是什么、为什么以这种形式呈现（见设计十二的证据卡片 `motive_check` 字段） |
| **可逆性与反馈周期** | 改错了可以 revert，反馈以秒/小时计 | 投资决策基本不可逆，且最重要的反馈（判断对错）以年计 | "快速迭代"哲学的迁移有硬边界：**研究流程可以迭代，决策不能**。这强化了 Spec-Driven 的价值（决策前把验证做完），但削弱了 Loop 自动化的适用范围（自动化只能用于信息收集，不能用于判断）。判断质量的改进只能靠决策日志拉长周期回填（见设计十一） |

### 2.2 由此产生的三个设计修正

1. **验证分层**：全文凡提到"验证"，先问是哪一层——事实、推理、还是判断。自动化工具只能处理前两层；第三层归人和时间。
2. **证据生成机制分析**：对抗性环境里，证据的来源和呈现方式本身就是证据。设计十二把 `motive_check` 设为证据卡片的必填字段。
3. **迭代边界**：Research Loop（设计三）只自动化"信息收集和整理"，所有涉及判断的节点必须有人审检查点。

### 2.3 本文档范围声明

- **流程设计聚焦投前**：从看到 deck 到投决会/打款的全部流程设计（设计一到十、十三）面向投前研究。
- **数据架构预留投后接口**：设计十二的决策层与反馈层覆盖投后场景（持有/加仓/减仓/退出、定期复盘、memo diff），投后工程化落地时数据层无需返工。
- **组合层视角**在设计十一（决策日志）与设计十二（跨项目实体图谱）中展开——组合层的核心价值恰好是判断校准数据与跨项目模式识别的来源。

### 2.4 类比使用原则

后续每个设计章节都默认通过了这个检查：**"如果此处软件工程类比失效，设计是否仍然成立？"** 不成立的部分已在正文标注边界条件。

---

## 3. 设计一：从 Prompt-Driven 到 Skill-Driven 的完整跃迁

### 3.1 核心洞察

> **Prompt 是临时的，Skill 是持久的。** —— 第 1 章

你每次对 AI 说"帮我做这个项目的竞品分析"，得到的结果质量取决于你当时的表达、AI 当时的理解、对话上下文的完整度。下次做另一个项目的竞品分析，一切从零开始。

而一个 Skill——比如 `/jvc-comps-dd`——是一个结构化指令文件，定义了"什么是好的竞品分析"、覆盖哪些维度、遇到每种情况怎么处理。它可以在不同项目、不同时间重复使用。

**当前的 `/jvc-*` 技能体系已经完成了 Skill-Driven 的雏形**，但设计深度上可以借鉴更多。

### 3.2 借鉴 Matt Pocock：四个失败模式与四个修复

Pocock 的 Skills 系统基于对 AI 失败模式的精确诊断。投资研究场景存在完全同构的失败模式：

| AI 编码失败模式 | 投资研究失败模式 | 修复 Skill 设计方向 |
|---------------|----------------|-------------------|
| **Agent 没做我想要的事** | AI 分析偏离研究意图，遗漏关键维度 | `/jvc-grill-me`：穷举式盘问研究计划，一次只问一个问题，沿决策树每个分支走到底 |
| **Agent 过于啰嗦 / 用词不精确** | AI 堆砌无关信息，或用模糊词替代精确判断 | 两件配套的事：**输出约束**治啰嗦（篇幅上限、表格优先、结论前置，写入各 SKILL.md 的输出规范）；**CONTEXT.md** 治模糊（领域词汇表和判断标准，让 AI 用精确术语而非绕圈子） |
| **代码跑不起来** | 分析结论经不起推敲，证据链断裂 | `/jvc-tdd`：垂直切片式验证——一个论断→一个证据→验证通过→下一个论断 |
| **我们构建了一团浆糊** | 多轮分析后逻辑不自洽，前后矛盾 | `/jvc-architecture-check`：周期性检查整个分析框架的连贯性，识别矛盾 |

### 3.3 具体设计：投资研究领域的 Skill 增强

> **命名约定**：所有新建 skill 统一 `jvc-*` 前缀（不再混用 `research-*`），创建时同步更新 `library/skill-registry.md` 和 README 工具总览表。

#### 3.3.1 `/jvc-grill-me` — 研究前的需求对齐

**行为定义**（借鉴 `/grill-me`）：
> 无休止地盘问我关于这个研究计划的每一个方面，一次只问一个问题。沿着决策树的每一个分支走到底，不跳过任何一个。对每个问题，给出你的推荐答案。

**投资研究场景示例**：
- "这个项目的核心竞争力是什么？"→ 推荐答案：创始人自述是 X，但需要从客户访谈验证
- "市场规模用自上而下还是自下而上的方法？"→ 推荐答案：自下而上，因为这是垂直 SaaS，可以用客户数 × ARPU
- "最需要验证的三个假设是什么？"→ 逐一确认优先级
- "哪些竞品是我们确定要对标的？"→ 逐个确认竞品名称和关注维度

**增强版 `/jvc-grill-with-docs`**（借鉴 `/grill-with-docs`）：
- 将讨论中产生的新术语和判断标准写入项目的 `CONTEXT.md`
- 将任何不可逆的研究决策（如"本次不覆盖海外市场"）记录为 ADR（Architecture Decision Record，架构决策记录）

#### 3.3.2 `CONTEXT.md` — 项目的共享语言和判断标准

**借鉴 Pocock 的"统一语言"概念：**

```markdown
# CONTEXT.md — XX项目

## 领域术语
- 核心指标：客户侧的"活跃"定义为过去30天内至少完成X次操作
- 竞品分类：一类竞品（直接竞争）、二类竞品（替代方案）、参照标的（不同市场但模式相似）
- 估值锚点：本轮谈判的核心锚点是上次融资的post-money vs 可比公司PS倍数

## 判断标准
- "产品有壁垒"的验证标准：至少3个客户说"没有这个产品我们会有大麻烦"
- "团队强"的评估维度：行业经验、学习速度、招聘能力、利益分配
- "市场够大"的定义：TAM > 100亿人民币且SAM > 10亿

## 已关闭的决策
- D-01: 本次研究聚焦国内市场，不纳入海外对标
- D-02: 财务模型中的收入预测采用保守情景（年增长30%）作为基准
- D-03: 竞品对比重点看产品维度而非财务维度（目标项目未公开财务）
```

**价值**：每次新会话、新 Agent 启动时自动读取，不需要重新"教"项目里的词和标准。知识复利生效。

**腐化防护**：文件头部带 `last_verified` 日期；已关闭决策被后续发现推翻时，标记为"已重开"而非直接删除——保留决策演变史（见设计九的文件腐化对策）。

#### 3.3.3 `/jvc-handoff` — 研究会话的交接

**借鉴 `/handoff` 的六个设计决策：**

1. **"不要重复已回答的问题"** — 交接文档引用已有工件（research-plan、尽调记录、ADR），不重新抄写
2. **"建议下一步要用的 Skill"** — 如"下一步运行 `/jvc-comps-dd`"
3. **"保存到临时目录，不是工作区"** — 研究暂存文件与正式交付物分离
4. **"自动脱敏"** — 交接文档不出现 API key、密码、被投企业敏感信息
5. **"按用户描述的下一个会话目标来定制"** — 如运行 `/jvc-handoff "继续做竞品尽调"`

**一个典型交接场景：**
> 今天做了初筛，对齐了 5 个关键假设并确定了优先验证顺序。CONTEXT.md 已更新。研究计划在 `spec/research-plan.md`。下一步运行 `/jvc-comps-dd`，重点关注 A 公司和 B 公司的产品差异。

**实现方式**：本机已安装 gstack 的 `/handoff`，`/jvc-handoff` 在其基础上包一层 jvc 语义（项目路径约定 + 脱敏规则），不重复造轮子（见附录 B）。

---

## 4. 设计二：Spec-Driven Research——研究规格驱动

### 4.1 核心洞察

> **规格不是在浪费时间写文档——规格是你和 AI 之间最有效率的通信协议。** —— 第 3 章

SDD（Spec-Driven Development）的核心主张在投资研究中的映射：**在开始研究之前，先定义"研究做完了"意味着什么。**

当前投资研究的典型问题：
- 研究过程中不断发现"原来这个也需要查"，范围蔓延
- AI 生成的 memo "看起来都对"，但缺了关键维度
- 不同人对"深度研究"的理解不同，产出质量方差极大

### 4.2 三层递进：从 Spec-First 到 Spec-as-Source

| 层级 | 软件工程 | 投资研究 | 当前状态 |
|------|---------|---------|---------|
| **Spec-First** | 写代码前先写要做什么 | 研究前先定义研究范围和验收标准 | 大部分项目应该至少做到这层 |
| **Spec-Anchored** | 规格与代码同步演进 | 研究过程中规格随发现动态更新 | 理想状态 |
| **Spec-as-Source** | 规格是唯一人工编辑的产物，代码自动生成 | 研究框架/假设体系是唯一人工维护的，报告自动合成 | 远期目标 |

### 4.3 权威目录结构（全文唯一标准）

此前文档存在三套互不兼容的目录约定（README 编号结构、OpenSpec 风格 `spec/changes/`、GSD 风格 `evidence/analyses/outputs/`）。现统一为以下**唯一权威结构**——以 README 编号约定为外壳（用户已熟悉），`spec/` 为新增内核：

```
projects/<项目名>/
├── 00-source/                  # 只读区：deck、财务表、转写、访谈 .docx（README 约定不变）
├── spec/                       # 研究规格内核（新增）
│   ├── CONTEXT.md              # 共享语言和判断标准（L1+）
│   ├── research-plan.md        # 研究范围和验收标准（L1+）
│   ├── hypotheses.md           # 核心假设清单，带验证状态（L1+）
│   └── tasks.md                # 可验证的研究任务清单（L2+）
├── STATE.md                    # 多轨道研究状态总览（L2+，见设计七）
├── decision-journal.md         # 决策立案与结果回填（L3 决策时，见设计十一）
├── evidence/                   # 证据库，证据卡片见设计十二（L2+）
│   ├── customer-interviews/
│   ├── market-reports/
│   ├── competitive-intel/
│   └── financial-data/
├── 01-prescreen.md             # ← /jvc-prescreen（README 编号约定不变）
├── 02-dd-notes.md              # 用户自己的尽调笔记
├── 03-founder-sync.md          # 用户自己的访谈笔记
├── 04-bull-case.md             # ← /jvc-bull-case
├── 04-bear-case.md             # ← /jvc-bear-case
├── 05-comps-dd.xlsx            # ← /jvc-comps-dd
├── 05-market-sizing.xlsx       # ← /jvc-market-sizing
├── 05-roi-modeler.xlsx         # ← /jvc-roi-modeler
├── 06-ic-memo.md               # ← /jvc-ic-memo
└── 99-decision.md              # 用户自己的最终决策
```

- 各级别的强制工件清单见设计十三（L0 快筛不需要 `spec/` 里的大部分文件）。
- L3 深度尽调如需增量研究任务管理，可在 `spec/` 下加 `changes/<研究任务名>/`（OpenSpec 风格：proposal + delta specs + design + tasks），非 L3 不启用。

**维护责任**（谁维护什么，是防腐化的第一道工序）：

| 文件 | 维护者 | 更新时机 |
|------|--------|---------|
| `spec/CONTEXT.md` | 人定标准，AI 记录 | grill-me 对话后、新术语出现时 |
| `spec/hypotheses.md` | AI 更新验证状态，人确认 | 每批证据入库后 |
| `spec/tasks.md` | AI 起草，人排优先级 | 阶段规划时 |
| `STATE.md` | AI 维护，脚本校验 | 每个节点状态变更时 |
| `decision-journal.md` | **人写判断**，AI 提醒回填 | 决策时 + 每 6 个月 |
| `evidence/` | AI 入库，人抽查 | 持续 |

### 4.4 研究规格示例

**`spec/hypotheses.md`**（核心假设清单——假设即认识论类型为"待核查/模型推算"的 Claim，统一 schema 见设计十二 14.2）：

```markdown
# 核心假设清单

## H1: 市场假设 — 国产替代需求将在未来3年释放至少50亿市场
- 状态: [未验证]
- 验证方法: 访谈5家潜在客户 + 行业报告交叉验证
- 证伪条件: 如果3家以上客户表示"现有方案够用，没有切换意愿"
- 证据: [待收集]（证据卡片 schema 见设计十二）

## H2: 产品假设 — 产品在核心性能指标上领先竞品12-18个月
- 状态: [部分验证]
- 验证方法: 客户访谈 + 竞品产品实测 + 技术专家评估
- 证伪条件: 如果任一竞品在2个以上核心指标上持平或超过
- 证据:
  - [E-001] [客户A] "切换后处理速度提升了3倍" [客户访谈 2026-07-15，待交叉验证]
  - [E-002] [未核实] 竞品B的产品路线图显示类似功能预计2027Q2上线

## H3: 团队假设 — 创始团队具备从0到1的商业化能力
- 状态: [未验证]
- 验证方法: 背调前同事/合作伙伴 + 客户评价 + 历史业绩追踪
- 证伪条件: 如果前两次创业的客户获取成本趋势不降反升
- 证据: [待收集]
```

**`spec/tasks.md`**（可验证的研究任务）：

```markdown
# 研究任务清单

## T1: 市场规模验证 (依赖: 无)
- [ ] 获取并阅读艾瑞/IDC 相关报告（2024-2026）
- [ ] 自下而上测算：潜在客户数 × 渗透率 × ARPU
- [ ] 访谈3位行业人士验证测算假设
- **验证标准**: 两种测算方法结果差异 < 30%
- **关联假设**: H1
- **预计时间**: 2天

## T2: 竞品产品力对比 (依赖: 无)
- [ ] 实测3家直接竞品的核心功能
- [ ] 整理功能对比矩阵（至少20个维度）
- [ ] 访谈至少2家竞品的客户了解切换意愿
- **验证标准**: 每个竞品至少5个客户评价来源
- **关联假设**: H2
- **预计时间**: 3天

## T3: 客户验证 (依赖: T1, T2)
- [ ] 访谈5-8家目标客户（包含已签约和未签约）
- [ ] 了解采购决策流程、对比过的竞品、核心选择因素
- [ ] 验证"客户粘性"——问"如果涨价30%你还会用吗"
- **验证标准**: 至少3个客户访谈包含完整采购决策链
- **关联假设**: H1, H2
- **预计时间**: 5天

## T4: 团队背调 (依赖: 无)
- [ ] 联系创始人前同事/合作伙伴（至少3人）
- [ ] 核查创始人自述的过往业绩
- [ ] 了解团队稳定性（核心成员留存率、利益分配）
- **验证标准**: 每个创始人至少2个独立背调来源
- **关联假设**: H3
- **预计时间**: 3天
```

### 4.5 SDD 六条原则在投资研究中的映射

| 软件工程原则 | 投资研究原则 |
|-------------|-------------|
| **规格先行，代码在后** | **研究框架先行，调研执行在后** — 先定义什么算"研究充分"，再开始收集信息 |
| **规格是活文档** | **假设清单是活文档** — 每条假设的验证状态随研究推进持续更新 |
| **规格粒度适中** | **研究任务粒度适中** — 一个任务应该在半天到两天内可验证完成 |
| **规格必须可验证** | **假设必须可证伪** — "团队强"不是假设，"团队在X能力上达到Y标准"才是 |
| **规格应与平台解耦** | **研究规格用纯 Markdown** — 不绑定任何特定 AI 工具 |
| **选择与任务匹配的层级** | **按研究分级选择规格密度** — 操作化方案见设计十三 |

---

## 5. 设计三：Research Loop——自主循环研究引擎

### 5.1 核心洞察

> **Ralph Loop 的核心贡献不在于技术复杂度，而在于找到了人类意图和 AI 执行之间的最小可行控制结构。** —— 第 4 章

```
while :; do
  cat RESEARCH_PROMPT.md | ai-agent --continue
done
```

这个看似粗糙的 Bash one-liner 蕴含了一个深刻洞见：**AI 第一次的产出通常不对，但如果让它读到自己的上一轮产出、发现差距、继续改进——它最终能到达正确的结果。** 关键不在循环本身，在于**自指涉机制**：每次循环中 AI 接收的是相同的 prompt，但它看到的东西变了——它的上一轮工作已经持久化在文件系统里。

**边界条件**（来自设计零）：Loop 只能自动化**信息收集和整理**，不能自动化**判断**。所有涉及判断的节点（假设标记为已验证、风险评级调整）必须走人工确认。

### 5.2 投资研究中的自主循环场景

| 场景 | 循环模式 | 出口条件 |
|------|---------|---------|
| **信息收集循环** | AI 搜索→发现缺口→补充搜索→再发现缺口→… | "所有假设都有至少N个独立来源" |
| **证据验证循环** | AI 整理证据→交叉验证→发现矛盾→深入核查→… | "至少80%的关键论断有双重验证" |
| **Memo 打磨循环** | AI 生成初稿→对照规格自查→发现遗漏→补充→… | "每条验收标准都有对应段落且证据标注完整" |
| **竞品监控循环** | 定时扫描竞品动态→发现变化→更新对比矩阵→… | 时间驱动（每周一次）或事件驱动（竞品有新融资/产品更新） |

竞品监控循环的输出接入设计十二的反馈层：新证据 → SUPERSEDES 受影响 Claim → 重算论点与情景 → 输出 memo diff 与下一步行动。

### 5.3 双出口门设计

借鉴 Ralph Loop 的 Promise + Max-Iterations 双出口：

```markdown
---
active: true
iteration: 1
max_iterations: 20
completion_promise: "RESEARCH_COMPLETE"
started_at: "2026-07-30T09:00:00Z"
---

# 研究任务：验证XX项目的产品-市场匹配

## 验收标准（promise 触发条件——全部满足才输出 RESEARCH_COMPLETE）
1. 至少访谈了5家付费客户，了解其采购决策过程
2. 至少访谈了3家流失客户或选择竞品的客户，了解不选的原因
3. 净推荐值(NPS)估算有至少10个数据点支撑
4. 客户留存率/续费率有可量化证据（不依赖创始人自述）
5. 产品切换成本有客户侧的定性+定量描述
6. 以上所有发现已更新到 hypotheses.md，每条假设标注验证状态

## 研究方法
- 客户访谈使用标准问卷模板 `/templates/customer-interview.md`
- 每个访谈结束后 2 小时内完成纪要
- 所有客户原话标注 `[客户原话]` 标签，与 AI 解读明确区分

## 逃生舱（15轮后如仍未完成）
- 记录阻塞每个验收标准的具体原因
- 列出所有已尝试的信息收集渠道
- 建议替代验证路径
```

### 5.4 两个投资研究特有的护栏（原书 Loop 没有）

1. **人在环检查点**：每 5 轮（或触及关键判断，如"某假设将被标记为已验证"）强制暂停，输出中间状态请人审。高风险研究不允许无人看管跑满 20 轮——在对抗性信息环境里，循环越久，被污染证据累积的风险越大。
2. **预算护栏**：每个循环设定 token / 时长上限，超限即触发逃生舱。成本失控本身就是"研究方向或方法错了"的信号。

**promise 的核实机制**：`RESEARCH_COMPLETE` 不由 agent 自评达成——与设计七的现实锚点共用一套机制：锚点文件存在且通过 lint 检查（如"5 份访谈纪要存在于 `evidence/customer-interviews/`"），promise 才算满足。Agent 声称完成不算数。

### 5.5 与现有 `/jvc-*` 的整合

```
/jvc-prescreen (初筛规格定义)
    ↓
/jvc-research-loop (自主循环执行信息收集和验证，带人审检查点)
    ↓
/jvc-bull-case + /jvc-bear-case (基于验证后的假设生成论点)
    ↓
/jvc-ic-memo (合成最终 memo)
```

`/jvc-research-loop` 是 Ralph Loop 的投资研究版本——它不替代 `/jvc-prescreen` 和 `/jvc-ic-memo`，而是填补中间的**执行空白**。

---

## 6. 设计四：虚拟投委会——多角色审查体系

### 6.1 核心洞察

> **gstack 的核心贡献：将二十三个专家角色写成二十三个 Markdown 文件，每个定义一种专门的"认知模式"。不是让一个全能 Agent 做所有审查，而是让二十三个专家各审查自己擅长的维度。** —— 第 5 章

当前投资研究的一个核心痛点：分析质量高度依赖个人判断，没有结构化的"第二意见"机制。gstack 的启示是：**给 AI 不同的角色人格，它会用不同的视角审视同一个问题。**

但有一个必须正视的风险（原书未展开）：**同一模型扮演全部角色时，产生的往往是"表演化的反对"**——语气不同，盲区同构。真正的对抗性来自信息和模型的差异，不是来自人设台词。本节 6.4 给出对策。

### 6.2 虚拟投委会角色设计

#### 核心审查角色（每次必用）

| 角色 | 人格设定 | 核心审查维度 | 对标 Skill |
|------|---------|-------------|-----------|
| **挑剔的 LP** | 你是某家族办公室的 LP，投过 50+ 基金，见过几百个项目。你的钱是真金白银，亏不起。 | 这个项目的风险 adjusted return 是否值得？portfolio fit 是否合理？管理层是否值得信任？ | `/jvc-bear-case` 增强版 |
| **竞品 CEO** | 你是目标项目最大竞品的 CEO。你比任何人都了解这个市场的坑在哪里。 | 为什么这个团队做不成？我们在哪些方面已经领先？市场的真正壁垒是什么？ | `/jvc-bull-case` 的反面对手 |
| **行业老炮** | 你在这个行业做了 20 年，看过三波创业潮起潮落。你对"风口"有天然的怀疑。 | 这个时间点对吗？为什么不是三年前也不是三年后？真正的行业拐点信号是什么？ | 新增 |
| **IC 老大** | 你是投委会主席，最终签字的人。你的核心问题是：这笔投资如果失败，最可能的原因是什么？这个原因我们验证过了吗？ | 投资逻辑链中哪个环节最脆弱？团队/市场/产品/竞争/退出——哪个维度证据最薄？ | 新增 |

#### 专题审查角色（按需使用）

| 角色 | 使用场景 |
|------|---------|
| **财务侦探** | 审查财务数据的一致性和合理性，对标可比公司 |
| **技术架构师** | 审查技术壁垒的可持续性、技术路线的合理性 |
| **组织心理学家** | 审查创始团队的决策模式、冲突处理方式、权力结构 |
| **监管专家** | 审查政策风险、合规成本、准入门槛变化趋势 |
| **退出策略师** | 审查可能的退出路径（IPO/并购/老股转让）的概率和估值区间 |

### 6.3 审查流程设计

借鉴 gstack 的七阶段 Sprint，设计投资研究的审查流程：

```
Define (定义研究范围)
   ↓
Research (执行研究——循环 + 人工调研)
   ↓
Bull Case Draft (多角色构建投资亮点)
   ↓
Bear Case Challenge (多角色反向论证)
   ↓
Cross-Examination (角色间交叉质询)
   ↓
Synthesis (整合分歧，形成投资逻辑)
   ↓
Memo & Decision (输出 memo，供人决策)
```

### 6.4 交叉质询设计（让对抗真实发生）

**发言顺序**：
1. Bull 方先陈述，每条亮点必须引用证据编号（E-xxx），不许用形容词
2. Bear 方逐条攻击，攻击必须指向证据链的具体环节（来源不可靠 / 推理跳跃 / 样本偏差）
3. Bull 方回应——只许补充证据，不许用修辞
4. 行业老炮与 IC 老大质询双方（"这个论据在 2015 年那波也听过，后来怎么证伪的？"）
5. 合成者整理输出

**私有信息注入**（关键设计）：每个角色只收到与其视角相关的**证据子集**——竞品 CEO 只收到竞品情报和客户流失访谈；挑剔 LP 只收到回报模型和组合上下文。让分歧来自**证据差异**而非语气差异。这模拟了真实投委会的信息不对称，也呼应设计零的"对抗性环境"原则。

**三条防表演化规则**：
1. 合成者必须是**独立会话**（最好是不同模型），且不参与任何一方的生成——对应设计十的 maker-checker 分离
2. 至少一个核心角色用**不同模型**跑——Claude 看不出的问题另一个模型可能看得出
3. 给至少一个角色投喂**真实历史失败案例库**（本基金的决策回填数据，见设计十一），让反对意见有实证根基而非凭空想象

**输出不是"共识"，而是分歧清单**：
- **真分歧**（证据冲突、判断标准冲突）→ 转化为 `spec/tasks.md` 里的待验证任务
- **假分歧**（定义不清、术语不一致）→ 回 `CONTEXT.md` 补术语定义

### 6.5 关键原则：角色分离 ≠ 追求数量

借鉴 gstack 的核心教训：**不是角色越多越好，而是每个角色必须覆盖一个人类审查者容易遗漏的维度。** 四个核心角色覆盖了"想投的理由""不投的理由""行业时机""最终决策逻辑"，已经能形成有意义的对抗性对话。

---

## 7. 设计五：Compound Research——让每份研究都让下一份更容易

### 7.1 核心洞察

> **不是让 Agent 这次做得更好，而是让 Agent 下次起点更高。每轮工作结束时，把学到的东西沉淀回知识库，变成下一轮 Agent 启动时自动读到的上下文。** —— 第 15 章

投资研究领域的"知识散落"问题比软件工程更严重：
- 每个项目的研究成果散落在 memo、邮件、聊天记录、笔记本里
- 同一赛道的不同项目之间，竞品信息、市场规模测算、行业规律本可以复用，但实际上每次都从零开始
- 一个 analyst 踩过的坑（"这个行业的付费用户数不能看注册数，要看月活"）是隐性知识，无法传给下一个项目

**边界条件**（来自设计零）：Compound 循环能复利**流程知识**（方法论、行业知识、数据源），但不能自动复利**判断质量**——后者的反馈周期以年计，需要决策日志专门处理（见设计十一）。两者互补，缺一不可。

### 7.2 Compound Research 循环

借鉴 Compound Engineering 的六步循环：

```
/ce-strategy (上游锚点，一次性或定期更新)
    ↓
/ce-brainstorm → /ce-plan → /ce-work → /ce-code-review
                                            ↓
                                       /ce-compound (沉淀)
                                            ↓
                              学到的东西沉淀回知识库
                              下一轮自动读到
```

投资研究版本：

```
STRATEGY.md (研究策略：聚焦赛道、投资阶段、核心判断标准)
    ↓
research-plan → 执行研究 → 验证发现
                            ↓
                    /jvc-compound (沉淀)
                            ↓
            方法论改进 + 行业知识积累
            下一次研究自动受益
```

### 7.3 `/jvc-compound` — 强制沉淀

**每次研究任务结束时的必做动作**（借鉴 `/ce-compound`）。**触发级别**：L2 及以上强制，L1 可选（见设计十三）——避免给轻量研究增加不成比例的开销。

```markdown
# 本阶段研究沉淀 — XX项目竞品尽调

## 方法论改进
- 发现：通过客户访谈了解竞品切换意愿比看产品功能对比更有效
- 原因：客户实际切换行为与"声称的意愿"差距很大
- 未来应用：竞品尽调中客户访谈权重应 > 产品功能对比

## 行业知识积累
- 发现：XX行业的定价模式从"按量收费"向"按效果收费"转变
- 证据：3/5个访谈客户提到"更倾向于效果导向的定价"
- 关联赛道：同样正在经历数字化转型的传统行业Y、Z

## 被证伪的假设
- 假设：竞品A的客户流失主要是因为价格
- 实际：客户流失的主要原因是产品稳定性 + 服务响应速度
- 教训：不要先入为主假设价格是主要竞争维度

## 新增约束/Checklist
- 竞品客户访谈必须包含："你考虑过换回原来的方案吗？如果是，为什么没换？"
- 竞品对比矩阵必须包含：客户支持响应时间、产品稳定性/可用性
```

### 7.4 `STRATEGY.md` — 研究策略的上游锚点

借鉴 Compound Engineering 的 STRATEGY.md 和 Loop Engineering 的 vision.md：

```markdown
# STRATEGY.md — 投资研究策略

## 我们投什么
- 阶段：Pre-seed ~ Series B，首次机构投资或早期领投
- 赛道：企业服务、AI 应用、跨境出海
- 地域：中国市场为主，适当覆盖东南亚

## 我们不投什么
- 模式创新的纯烧钱项目（单位经济模型不成立）
- 创始人股权高度分散或核心团队有明显裂痕
- 政策风险不可量化或不可对冲的项目

## 我们的判断标准
- "好市场"：TAM > 100亿，年增速 > 20%，且我们能在2年内看到拐点信号
- "好产品"：至少3个付费客户说"不用这个产品我们会有大麻烦"
- "好团队"：创始人在做的事是其人生中不可不做的事（而不只是"一个好的商业机会"）
- "好时机"：市场教育已完成或正在加速，但竞争格局尚未固化

## 我们的研究纪律
- 每个项目必须有 bear case，且 bear case 的篇幅不能少于 bull case
- 所有关键论断必须有至少两个独立来源
- 创始人自述的内容必须标注 `[创始人自述]`，除非有外部验证
- 不确定的事情写"不确定"，不写"可能""大概""应该是"
```

这个文件成为所有下游研究 Skill 的默认上下文——每个 `/jvc-*` 命令启动时自动读取，确保研究方向不偏离基金策略。

---

## 8. 设计六：反合理化表——阻止 AI 跳过研究纪律

### 8.1 核心洞察

> **Agent 不是在偷懒，而是用统计学上最可能的路径完成任务。跳过不擅长的步骤、直奔擅长的步骤，是概率问题。解法不是让 Agent 更聪明，而是让 Agent 无法说服自己跳过步骤。** —— 第 16 章

Addy Osmani 的 agent-skills 最锋利的创新是**反合理化表（anti-rationalization table）**——预判 Agent 会找什么借口跳过纪律，提前写下反驳。

投资研究场景下，AI 的合理化借口更加隐蔽和危险。

### 8.2 投资研究反合理化表

| AI 可能会说 | 预设的反驳 | 来源原则 |
|------------|-----------|---------|
| "这个市场很大，不需要精确测算" | 市场规模是估值锚定的核心输入。误差每增加 10%，估值区间就漂移一个数量级。不确定可以写"不确定"，但不能用模糊描述替代测算 | CLAUDE.md 红线 |
| "创始人背景很强，背调可以简化" | 投资历史上最大的亏损案例中，绝大部分涉及对创始人的误判。强背景恰恰意味着背调要更深入——因为赌注更大 | 反例驱动 |
| "竞品分析可以用公开信息，不需要客户验证" | 公开信息是竞品想让外界看到的。客户口中的竞品才是真实的竞品。两者差异本身就是重要信号 | 信息不对称原则 |
| "这个风险很小，不需要展开" | "小"的判断依据是什么？如果是基于直觉，写下来并标注 `[推测]`。如果是基于数据，把数据拿出来 | 证据标准 |
| "访谈纪要太长，我帮你总结了要点" | **压缩即立场**——总结时选择保留什么、丢弃什么，本身就是解读。原始逐字稿必须保留，关键引用必须标注原话。纪要生成后，抽查 10% 段落与逐字稿对照，确认没有因压缩而改变含义 | 一手资料完整性 |
| "这个项目跟前一个很像，可以用类似的结论" | 相似不等于相同。列出至少 3 个关键差异点。没有经过差异分析就复用结论，这是类比谬误 | 反模式识别 |
| "财务模型里的这个假设是行业惯例" | "行业惯例"是最大的黑箱。写出这个假设的具体数值、来源、以及如果假设偏乐观/悲观 20% 结论会怎么变 | 敏感性要求 |

### 8.3 在每个 Skill 中嵌入反合理化逻辑

**以 `/jvc-prescreen` 为例**，在 Skill 指令文件中加入：

```markdown
## 反合理化约束

在输出初筛纪要前，逐条自查以下借口是否出现在你的推理中。如果出现了，回到对应步骤重新执行：

1. "这个维度信息不足，先跳过" 
   → 不可以跳过。信息不足就写"信息不足"，并标注需要什么信息、从哪里获取。
   
2. "基于行业惯例，这个数据大概是X"
   → 不可以。没有来源的数据不用"大概"包装。写 `[未核实]` 或 `[需要用户提供]`。
   
3. "创始人在这方面很有经验，风险可控"
   → 不可以。经验不等于能力。列出创始人过往的具体成就或失败，让事实说话。
   
4. "市场很大，天花板不用担心"
   → 不可以。每个市场都有天花板。给出 TAM/SAM/SOM 的具体数字和来源。

5. "综合来看，这个项目值得关注"
   → 不可以。这是终局判断。你只负责摆证据、提问题，不负责下结论。
```

---

## 9. 设计七：Graph Research——并行化多轨研究编排

### 9.1 核心洞察

> **图工程的核心是设计循环之间的关系——先后顺序、并行可能性、数据流向。** —— 第 17 章

> **没有现实锚点，图就只是"被项目管理包装得更漂亮的大型幻觉"。每个节点的完成必须由外部产物定义。** —— 第 17 章

复杂投资项目的尽调天然是一个 DAG（Directed Acyclic Graph，有向无环图）：多个研究轨道并行，轨道间有依赖关系，最终汇聚到 IC memo。

### 9.2 投资研究 DAG 设计

以对一个 Series A 项目的完整尽调为例：

```
Wave 0 (地基):
  [市场空间测算] ── 独立，可立即开始
  [创始团队背调] ── 独立，可立即开始

Wave 1 (并行):
  [竞品产品力对比] ── 依赖：无，可与 Wave 0 并行
  [客户访谈 (已签约)] ── 依赖：无
  [客户访谈 (流失/未签约)] ── 依赖：无

Wave 2 (基于 Wave 0-1 结果):
  [产品-市场匹配判断] ── 依赖：客户访谈 + 竞品对比
  [单位经济模型] ── 依赖：市场空间 + 客户访谈中的付费数据

Wave 3 (汇聚):
  [bull case 构建] ── 依赖：Wave 0-2 所有轨道
  [bear case 构建] ── 依赖：Wave 0-2 所有轨道
  (bull 和 bear 可以并行——用不同的 Agent/角色)

Wave 4 (最终合成):
  [IC memo] ── 依赖：bull case + bear case + 所有原始证据
```

### 9.3 现实锚点：每个节点的完成由外部产物定义

借鉴第 17 章的核心警告——"agent 说做完了不算数"：

| 节点 | 完成锚点（必须可独立验证） |
|------|--------------------------|
| 市场空间测算 | `05-market-sizing.xlsx` 存在于项目根目录，含两种测算方法的 sheet |
| 创始团队背调 | `evidence/team-dd/` 含至少 3 个独立来源的背调记录 |
| 客户访谈 | 至少 5 份访谈纪要（`.docx`）存在于 `evidence/customer-interviews/` |
| 竞品对比 | `05-comps-dd.xlsx` 含至少 20 个对比维度 |
| Bull/Bear Case | `04-bull-case.md` 和 `04-bear-case.md`，Bear 篇幅 ≥ Bull 篇幅 |
| IC Memo | `06-ic-memo.md` 含完整证据标注，无 `[未核实]` 残留 |

**关键设计**：每个节点的完成状态不依赖 Agent 自我报告，而是由**文件系统上的真实产物**定义。这是图工程的"guard 边"——下一个节点能否启动，看锚点是否落地，而非上一节点声称完成。

**权限规则**（防止锚点被架空）：节点状态只能由**校验脚本**翻转——脚本检查锚点文件是否存在、是否通过 lint（如 xlsx 里是否真的有两个测算 sheet、访谈纪要份数是否达标）。Agent 可以**请求**状态变更，但不能自签 `complete`；`verified_by` 字段只接受人工复核记录。否则"现实锚点"会被 Agent 的自我报告重新架空。

### 9.4 状态文件设计

借鉴 GSD Core 的 `.graph_state` 和 STATE.md（落盘位置：项目根目录 `STATE.md`，见 §4.3 权威目录）：

```json
{
  "task": "XX项目 Series A 尽调",
  "waves": [
    ["market-sizing", "team-dd"],
    ["competitive-analysis", "customer-interviews-signed", "customer-interviews-churned"],
    ["product-market-fit", "unit-economics"],
    ["bull-case", "bear-case"],
    ["ic-memo"]
  ],
  "current_wave": 1,
  "nodes": {
    "market-sizing": {
      "title": "市场空间测算",
      "status": "complete",
      "anchor": "05-market-sizing.xlsx",
      "anchor_verified_by_script": true,
      "verified_by": "人工复核：TAM测算假设已确认"
    },
    "team-dd": {
      "title": "创始团队背调",
      "status": "in_progress",
      "anchor": null,
      "blocked_by": "等待创始人前同事回复"
    },
    "competitive-analysis": {
      "title": "竞品产品力对比",
      "status": "pending",
      "anchor": null
    }
  }
}
```

---

## 10. 设计八：Harness Engineering for Research——研究 Agent 的安全运行环境

### 10.1 核心洞察

> **模型不可预测，所以系统必须可预测。靠 prompt 里的"请勿"不顶用——靠 hooks 的 exit 2 blocking error。** —— 第 10 章

投资研究场景下的 Harness Engineering 关注点与软件工程不同，但原则相同：**AI Agent 能做什么、不能做什么，不应该依赖 prompt 里的"请勿"，而应该由系统强制执行。**

### 10.2 投资研究的 Harness 设计要点

#### 10.2.1 数据隔离

| 风险 | Harness 对策 |
|------|-------------|
| 将项目素材上传到外部 AI 服务 | 配置允许的外部域名白名单；只允许访问明确许可的公开 URL |
| 将 deck 内容泄露到对话外 | 防外泄的关键不在"禁读"而在"**禁外发**"：hook 扫描所有外发请求（WebFetch/WebSearch）的请求体，发现 `00-source/` 内容片段即阻断 |
| 无意中把被投企业数据混入其他项目 | 每个项目使用独立的 worktree 或会话隔离（可直接复用 gstack `/freeze` 锁定编辑范围到当前项目目录，见附录 B） |

#### 10.2.2 权限模型

```
# 研究 Agent 权限配置示例
allow:
  - Read(projects/**)              # 可以读所有项目文件（含 00-source——skill 的输入就在那里，禁读会让 skill 无法工作）
  - Write(projects/<current>/**)   # 只能写当前项目
  - WebFetch(https://*.itjuzi.com/**)   # 允许访问 IT 桔子
  - WebFetch(https://*.qichacha.com/**) # 允许访问企查查
  - WebSearch(*)                   # 允许搜索公开信息
deny:
  - Write(00-source/**)            # 绝对禁止写入原始素材区（CLAUDE.md 红线）
  - Bash(rm *)                     # 禁止删除任何文件
hooks:
  - PreToolUse(WebFetch|WebSearch):
      扫描请求体，若包含 00-source/ 文件内容片段或被投企业敏感信息 → 阻断
      # 这是防泄露的真正闸门：读是允许的，外发是拦截的
```

#### 10.2.3 验证 Hook

借鉴 Claude Code 的 PreToolUse hooks，在关键操作前设检查点：

- **写 memo 之前**：检查所有论断是否有来源标注
- **输出结论性语言之前**：拦截并提醒 CLAUDE.md 红线
- **对外发送信息之前**：检查是否包含敏感数据

---

## 11. 设计九：上下文工程——对抗信息过载

### 11.1 核心洞察

> **GSD Core 的核心敌人：上下文腐化（Context Rot）。上下文窗口越填越满，AI 输出质量随之下降。Agent 在对话开头思路清晰、引用准确；聊到第五十轮，开始忘记早先决定、混淆文件、把已否决方案重新提出。** —— 第 13 章

投资研究是典型的"信息密集 → 上下文腐化"场景：
- 一个项目的材料可能包含上百页 deck、几十份访谈纪要、多份行业报告
- 在一次长会话中，AI 很容易混淆不同来源的信息
- "上轮已经确认这个假设不成立，为什么这轮又当作成立来用了？"

### 11.2 借鉴 GSD Core 的对抗策略

#### 策略一：瘦编排者模式

**主会话（你直接对话的 Agent）只做四件事：**
1. 加载上下文（读 STRATEGY.md、CONTEXT.md、STATE.md）
2. 派生专门的子 Agent（派给各研究轨道）
3. 收集结果（读各子 Agent 的产出）
4. 更新共享状态（更新 STATE.md）

**重活全部下放给子 Agent：** 市场空间测算用一个子 Agent，竞品分析用另一个，客户访谈纪要用第三个。每个子 Agent 拿到一个干净的上下文窗口 + 只属于它的任务需要的材料。子 Agent 的上下文不会互相污染。

#### 策略二：文件化状态

目录结构统一执行 §4.3 的权威约定（spec/ + STATE.md + evidence/ + README 编号产出），此处不再重复。核心原则只有一条：**知识不活在对话上下文里，活在文件系统里。** 下次会话、下个 Agent 启动时，从文件系统冷启动，不是从零开始。

#### 策略三：阶段循环

借鉴 GSD Core 的 Discuss → Plan → Execute → Verify → Ship：

```
Discuss（对齐研究目标）
  → 产出：研究范围定义、核心假设清单
Plan（制定研究计划）
  → 产出：分轨任务列表、依赖图、方法论设计
Execute（执行研究）
  → 产出：访谈纪要、市场数据、竞品分析
Verify（验证发现）
  → 产出：证据链检查报告、矛盾标记、未覆盖缺口
Ship（交付成果）
  → 产出：IC memo、bull/bear case、证据索引
```

**铁律：每次只推进一个阶段。** 不允许一边收集信息一边写 memo。阶段之间清空上下文，用文件系统传递信息。

#### 策略四：对抗文件腐化（上下文腐化的慢性孪生兄弟）

上下文腐化是急性的（单会话内退化）；**文件腐化是慢性的**——CONTEXT.md 过期、hypotheses.md 的验证状态漂移、已关闭决策被推翻后没有更新。文件腐化不会报错，但会静默污染后续所有研究。对策：

1. **每个规格/状态文件头部带 `last_verified` 日期**，超过一个研究周期未复核的文件，读取时自动提示"此文件可能已腐化"
2. **定期运行 `/jvc-architecture-check`**（或每个研究阶段结束时）：跨文件查矛盾——假设状态与证据库是否匹配、CONTEXT.md 术语与正文用词是否一致、已关闭决策是否被后续发现推翻
3. **腐化即任务**：检查发现的每一处矛盾，转化为 `spec/tasks.md` 里的一条验证任务，而不是顺手改掉——改之前先弄清为什么会腐化，那往往本身就是一个研究信号

---

## 12. 设计十：验证闭环——从"AI 说了"到"证据表明"

### 12.1 核心洞察

> **Boris Cherny 提出的"验证差距"（Verification Gap）：AI Agent 编写补丁的速度，已经远远超过了人类组织验证它们的能力。** —— 卷首语

> **maker-checker 分离：写代码的模型给自己打分太客气了。第二个 Agent 带着不同的指令，有时候用不同的模型，能抓住第一个 Agent 自己说服自己的东西。** —— 第 12 章

投资研究中的验证差距更加致命：
- AI 可以在几分钟内生成一份看起来引经据典的行业分析
- 但人类验证这份分析中的每一个数字、每一条引用、每一个逻辑跳跃的时间，可能比从零自己写还长
- **核心矛盾：AI 提升了分析产出的速度，但验证速度没有同步提升。**

**边界条件**（来自设计零）：投资研究没有软件测试那样的客观裁判。因此验证必须先分三层，各层用不同机制——把三层混为一谈，是验证体系失效的根因。

| 验证层 | 验证对象 | 裁判 | 对应机制 |
|--------|---------|------|---------|
| **事实验证** | 数字、引用、日期、来源 | 可自动核对 | 下面的第一、二层 |
| **推理验证** | 因果链、口径一致性、逻辑跳跃 | 可审查但需解读 | 下面的第二、三层 |
| **判断验证** | 团队质量、市场时机、壁垒 | **只有时间能验证** | 第四层（人）+ 决策日志回填（设计十一） |

### 12.2 多层验证体系

#### 第一层：来源验证（自动化）

- 每个数据点是否标注了来源？
- 来源是否可追溯（URL/文件名/页码）？
- 来源是否在 `00-source/` 或证据库中实际存在？
- **工具**：类似 `openspec validate --strict` 的自动化检查，落地为 `scripts/` 下的 lint 脚本（见 12.3）

#### 第二层：一致性验证（AI 辅助）

- 同一份 memo 中，不同部分对同一事实的描述是否一致？
- 假设清单中的验证状态是否与证据库中的实际证据匹配？
- Bull case 中的论断是否与 Bear case 中的风险识别矛盾？
- **工具**：一个独立的验证 Agent（不同模型/不同会话），专门做"找茬"

#### 第三层：对抗性验证（多 Agent）

借鉴 autoresearch 的"多 Agent 轮转交叉审核"：
- Agent A 生成分析 → Agent B（攻击者角色）试图推翻 → Agent C 裁判
- 不同模型有不同的盲区——一个模型看不出的问题另一个可能看得出
- **关键**：审查用的 Agent 必须与生成用的 Agent 有不同的人格设定和验证指令（具体对抗设计见设计四 6.4）

#### 第四层：人类验证（不可替代）

- 关键数字（估值、市场规模、增长率）——人必须亲自核实原始来源
- 软性判断（团队质量、产品手感、市场时机）——只有人能做
- 终局决策——不允许 AI 替代

#### 第五层：历史回放（回归测试——投资研究的"测试 oracle"）

前四层都是"向前看"的验证。但投资研究最接近软件测试 oracle 的东西是：**用已知结局的历史项目回放研究流程**。

- 把过去项目的原始素材（deck、访谈纪要）喂给当前的 skill pipeline，看流程能否重现当初漏掉的盲区——当初踩过的坑，现在的流程还会再踩吗？
- 仓库已有基础设施雏形：`examples/`（每个 skill 的 golden 输出）和 `evals/trigger_cases.json`。历史回放是其自然延伸：`examples/` 防 skill 输出退化，历史回放防**研究方法论**退化
- **节奏**：每改动一次关键 skill（prescreen / bear-case / ic-memo），用 2–3 个已知结局的历史项目做回放，对比新旧输出的**盲区覆盖率**
- 这也是决策回填数据（设计十一）的用途之一：回填不仅校准判断，还积累回放测试集

### 12.3 验证检查清单自动化

借鉴 agent-skills 的"每一步以证据收尾"原则。以下清单应落地为**可运行的 lint 脚本**而非仅 Markdown 清单——写 memo 的 hook 自动调用，不通过则阻断输出：

```markdown
# Memo 质量门禁（借鉴 /review 的五轴审查）

## 正确性检查
- [ ] 所有数字是否有标注来源且来源可追溯？
- [ ] 所有引用是否在原文中存在且上下文一致？
- [ ] 所有计算是否可复现（公式 + 输入值明确）？

## 完整性检查
- [ ] Bear case 篇幅是否 ≥ Bull case 篇幅？（CLAUDE.md 硬约束）
- [ ] 是否列出了"未覆盖的关键问题"？（CLAUDE.md 硬约束）
- [ ] 每个核心假设是否都有验证状态标注？

## 一致性检查
- [ ] 同一事实在不同段落中的描述是否一致？
- [ ] 假设清单与正文分析是否吻合？

## 逻辑检查
- [ ] 是否存在"因为 A 所以 B"但没有解释因果链条的跳跃？
- [ ] 是否存在用形容词替代数据的表述？

## 规范检查
- [ ] 所有 `[来源类型]` 标注是否完整？
- [ ] 是否出现了 CLAUDE.md 禁止的终局判断语言？
- [ ] 英文缩写首次出现时是否附了中文解释？
```

---

## 13. 设计十一：决策日志与结果回填——判断质量的复利

> **本设计不来自原书——它处理的是投资研究相对软件工程的最大差异：反馈周期以年计。** Compound Research（设计五）复利流程知识，本设计复利判断质量。没有它，§16 成熟度模型的最高级"方法论自我进化"就是空话——没有结果数据，无法知道哪类假设验证方法真的有效。

### 13.1 核心洞察

软件工程的 Compound Engineering 之所以成立，是因为反馈快：改完代码立刻知道测试过不过。投资研究的终极反馈——这笔投资赚不赚钱——要 5–10 年才到达。**如果不在决策瞬间把"当时相信什么、为什么"冻结下来，事后复盘会被后见之明污染**（"我早就知道会这样"）。决策日志是对抗后见之明偏差的唯一工具。

这也补上全文唯一缺失的**组合层视角**：VC 是 power law 游戏，单个项目研究再工程化，没有跨项目的模式识别（"我在哪类团队/哪类市场上系统性误判"），工程化的价值减半。决策回填数据正是跨项目模式识别的原料。

### 13.2 决策立案模板（`decision-journal.md`，99-decision.md 决策时触发）

```markdown
# 决策立案 — XX项目（YYYY-MM-DD）

## 当时核心判断
- 判断：投 / 不投 / 等（投后动词：持有 / 加仓 / 减仓 / 退出 / 等待）
- 责任人：
- 依据的核心假设（引用 spec/hypotheses.md 编号，即 Claim 编号）：H1, H2
- 每条假设的置信度（0–100）：H1=65, H2=40
- 关键证据（引用证据编号）：E-001, E-007
- 有效期：本决策在哪些条件成立时有效
- 下一次复盘：YYYY-MM-DD（触发翻案信号则提前）

## 最大的担心
- （允许是直觉，标注 [直觉]；说不清具体原因也合法——记录触发这个感觉的具体瞬间。
   例："[直觉] 第二次见面时创始人对 CTO 的提问全程代答，当时觉得不对劲"）

## 翻案信号
- 出现什么信号时，这个判断应该被重新打开：
  （例：如果竞品 B 在 6 个月内上线同等功能，H2 需要重估）
- 与情景树的"触发器"共用一套机制（见设计十二 14.3）——翻案信号即情景切换信号

---
# 结果回填（每 6 / 12 / 24 个月）

## YYYY-MM-DD 回填
- 实际进展 vs 当初假设：
- 最先被证伪/证实的假设：
- 当初的 [直觉] 应验了吗：
- 沉淀到 tracks/{赛道}/ 的新知识：
- 是否产生新的回放测试素材（喂给设计十第五层）：
```

### 13.3 运行机制

1. **立案强制**：每个走完 L3 流程的项目（包括决定**不投**的——不投的判断同样需要校准）必须立案。L1/L2 放弃的项目可选立案（一句话版也行）
2. **回填节奏**：每 6 / 12 / 24 个月，或触发"翻案信号"时提前回填。可由 Research Loop（设计三）时间驱动提醒
3. **跨项目分析**：每年一次，把所有回填数据汇总——哪类假设我系统性高估？哪类直觉屡验不爽？这是次年 STRATEGY.md 修订的输入
4. **实现复用**：本机已有的 `dbs-decision` skill（决策立案 / 结果回填 / 状态画像）结构同构，直接复用其方法论，落盘到项目目录的 `decision-journal.md`（见附录 B）

### 13.4 与其他设计的关系

| 上游输入 | 下游输出 |
|---------|---------|
| `spec/hypotheses.md`（假设与置信度） | 设计十第五层：回填项目成为历史回放测试集 |
| `evidence/`（关键证据编号） | 设计五：回填发现的行业规律进 `/jvc-compound` |
| 设计四虚拟投委会（当时的分歧清单） | 次年 STRATEGY.md 修订 |

---

## 14. 设计十二：证据与主张图谱——从文档到决策的数据架构

> **本设计不来自原书具体章节。** 证据卡片 schema 只覆盖数据架构的第一层；v3 融入《投资研究决策图》的四阶段模型（研究输入 → 研究事实图 → 领域与分析图 → 决策与产出 + 持续反馈回路），把设计二（假设）、设计十（验证）、设计十一（决策日志）的地基统一为一张图。其中的软件工程通识类比——增量构建、版本控制、双时态数据——恰好呼应本书主题。

### 14.1 五层数据模型

```
[事实层]  Document ──contains──▶ Evidence ──supports / refutes──▶ Claim
                                  （人工验证闸门：AI 只做候选预填）
[领域层]  Claim ──ABOUT──▶ Entity（公司 / 人物 / 产品 / 客户 / 行业 / 指标…）
[分析层]  Claim 集合支撑 Thesis（行业论点 / 公司论点）与情景树（Bull / Base / Bear）
[决策层]  Thesis / 情景 ─▶ Memo（每个结论回链 Claim）─▶ Decision（活对象）
[反馈层]  新证据 ─▶ SUPERSEDES 旧 Claim ─▶ 重算受影响的 Thesis / 情景
          ─▶ 输出 Memo diff 与下一步行动
```

### 14.2 事实层：Document / Evidence / Claim

**Document**：路径 · 版本 · 日期 · 权限 · 文件指纹。原始资料不覆盖（`00-source/` 只读的数据层表达），敏感资料按权限隔离（对接设计八）。

**Evidence（证据卡片）**，每条证据通过**极性边**挂接到 Claim，而非自己"拥有"论断：

```markdown
# 证据卡片 E-007
- source: 来源（文件名 / URL / 访谈编号，含页码或时间戳）
  例：evidence/customer-interviews/【2026年07月15日访谈】客户C.docx, Q4
- method: 获取方式（一手访谈 / 公开报告 / 创始人自述 / 第三方转述 / 推测）
  例：一手访谈
- obtained_at: 获取日期    例：2026-07-15
- confidence: 高 / 中 / 低（附一句理由）    例：中——单一客户样本，待交叉验证
- 挂接: → Claim C-012，polarity = supports（supports / refutes / neutral）
- motive_check: 谁提供的？动机是什么？为什么以这种形式呈现？（必填）
  例：客户C 主动提及；但它是竞品A的流失客户，可能存在为切换找理由的倾向
- last_verified: 最近复核日期    例：2026-07-20
```

**Claim（可验证主张）**——独立成层，这是本次融入最重要的修正。原设计把 claim 内嵌在证据卡片里，导致一条论断无法被多条证据共同支撑、也无法被多个论点复用。schema：

```markdown
# Claim C-012
- 陈述: 主体 + 谓词 + 数值 + 单位 + 时间范围
  例：竞品A 2025年 年化客户流失率 28%
- 认识论类型: 事实 / 公司口径 / 模型推算 / 待核查
  例：公司口径（来自竞品A对外披露，未经客户侧验证）
- 证据状态: 候选(AI预填) / 已验证(人工确认) / 已替代(被 C-0xx SUPERSEDES)
- 置信度: 高 / 中 / 低
- 支持证据: E-007, E-019
- 反驳证据: E-003
- 公式: （认识论类型为"模型推算"时必填）
  例：流失率 = 年流失客户数 / 期初客户数；输入值及各自来源
- created_at / verified_by / last_verified
```

**三条硬规则**：

1. **人工验证闸门**：AI 抽取的 Claim 一律为"候选"，人工确认后才转为"已验证"——数据层的 maker-checker（设计十的原则下沉到事实入库环节）。
2. **模型推算必须保存公式**：可复现性从 memo 检查清单下沉到 Claim 层——没有公式和输入值的推算值不允许进入图谱。
3. **冲突证据并存**：supports 和 refutes 边同时保留，不强行二选一。同一 Claim 上同时存在两种极性的证据即冲突，自动进入冲突登记簿并生成 `spec/tasks.md` 验证任务（冲突本身是重要信号，原则不变）。

**与假设体系统一**：`spec/hypotheses.md` 的"假设"就是认识论类型为"待核查 / 模型推算"的 Claim（H 编号即 C 编号）。假设的验证状态由证据状态推导——所有关键证据"无未解决冲突 + 高/中置信度"时才可标记已验证，且仍需人工确认（判断层归人，见设计十）。

### 14.3 领域层与分析层

**Entity（领域实体）**：公司 · 人物 · 产品 · 客户 · 供应商 · 交易 / 行业 · 技术 · 市场 · 产业链 · 指标 · 财务。Claim 通过 ABOUT 边挂到实体上。

价值：**这是 Compound Research（设计五）的结构化基座**——同一竞品实体在新项目中自动带出既有 Claim 集合，跨项目复用不再只有 tracks/ 里的散文。存储格式与 `/jvc-knowledge-tree-builder` 的 `nodes.json` / `evidence_index.md` 对齐（见附录 B）。

**Thesis（论点）**：
- 行业论点：展望 · 驱动因素 · 瓶颈 · 可持续性
- 公司论点：护城河 · 飞轮 · 团队 · 产品 · 经济性
- 由 Claim 集合支撑，**支持与反驳关系同时保留**——Thesis 不是结论，是结论与其反面的当前平衡态

**情景树**：Bull / Base / Bear 三支，每支带**条件 · 概率 · 触发器**。触发器与决策日志的"翻案信号"共用一套机制（见设计十一）。

**风险 · 催化剂 · 证伪条件**：什么变化会推翻论点或触发行动——直接喂给 bear case（设计四）与决策立案（设计十一）。

### 14.4 决策层：memo 回链与活决策

**memo 回链原则**：IC memo 的每个结论必须解析到至少一条 Claim（编号引用）；无 Claim 支撑的结论不允许出现在 memo 中——lint 脚本可自动检查（设计十 12.3 的门禁扩展）。memo 章节结构仍由 `/jvc-ic-memo` 的十段式定义，图谱只要求回链。

**决策是活对象，不是一次性判决**。决策记录 = 行动动词 + 责任人 + 条件 + 有效期 + 下一次复盘日期：
- 投前动词：投 / 不投 / 等
- 投后动词：持有 / 加仓 / 减仓 / 退出 / 等待
- 这些字段并入 `decision-journal.md` 的立案模板（见设计十一 13.2）

### 14.5 反馈层：SUPERSEDES 与影响传播

软件工程类比：**这是构建系统的增量编译**——Claim 是中间产物，Thesis / 情景 / memo 是下游 target；一条 Claim 被替代时只重算受影响的 target，而不是重写整份 memo。**这也是 git**：Claim 有版本历史（SUPERSEDES 链），memo 有 diff。

机制（对应决策图的 05 反馈回路）：

1. 新证据到达 → 受影响的旧 Claim 标记 SUPERSEDES（保留历史，不删除——陈旧判断永远可溯源）
2. 沿 ABOUT / 支撑边找到受影响的 Thesis 与情景分支 → 重算概率与置信度
3. 输出 **memo diff**（哪些结论变了、为什么）+ 下一步行动清单
4. 触发源：设计三的监控循环、新尽调发现、投后定期复盘

**文件腐化（设计九策略四）的数据层解法**：文件会腐化，图不会——只要 SUPERSEDES 链完整，任何陈旧结论都能沿链追到它赖以成立的证据，并一次性重算所有受影响的下游。

### 14.6 落地约束（对齐设计十三）

| 级别 | 图谱深度 |
|------|---------|
| L0 / L1 | 无图谱，只做 memo 来源标注 |
| L2 | Claim 从简：memo 内编号引用 + `motive_check` 一句话；无实体图 |
| L3 | 完整五层；实体库跨项目存 `tracks/{赛道}/entities/` |

存储用 Markdown + JSON（与 `nodes.json` 约定一致），**纯文件系统即可跑通，不绑定图数据库**——图数据库是远期优化，不是前置依赖。防止图谱管理本身成为新的流程负担。

---

## 15. 设计十三：研究分级与流程密度——防弃用保险

> **本设计不来自原书——它防止方法论迁移的第一死因：流程密度失控。** 如果把本文档的全部设计无差别应用到每个项目，一个 2 小时的快筛也要维护 CONTEXT.md + hypotheses.md + tasks.md + STATE.md + 证据卡片 + compound 沉淀——人会直接放弃整个系统，回到裸奔状态。

### 15.1 四级研究分级

| 级别 | 场景 | 强制工件 | 流程开销上限 |
|------|------|---------|-------------|
| **L0 快筛** | 30 分钟看 deck，决定要不要花更多时间 | 无，只有 `/jvc-prescreen` 输出（`01-prescreen.md`） | 0 额外文件 |
| **L1 初筛** | 决定要不要见创始人 | + `spec/CONTEXT.md` 骨架、`spec/hypotheses.md`（3–5 条核心假设） | < 1 小时额外开销 |
| **L2 尽调** | 见面后决定深查 | + `spec/tasks.md`、`evidence/`（轻量来源标注 + motive_check）、虚拟投委会（核心四角色）、`/jvc-compound` 沉淀 | 按 DAG 执行（设计七），证据卡片从简 |
| **L3 重仓/领投** | IC 前 | + 完整证据卡片 schema、`STATE.md` 多轨道管理、决策立案（`decision-journal.md`）、全量验证门禁、历史回放对照 | 全量 |

### 15.2 三条运行规则

1. **每个 SKILL.md 声明最低适用级别**：如 `/jvc-prescreen` 适用 L0+，`/jvc-roi-modeler` 适用 L2+，决策立案适用 L3。用户在低级别项目里调用高级别 skill 时，skill 应提示"当前项目为 L1，本 skill 的完整流程是否必要？"
2. **砍流程不砍纪律**：流程开销可以按级别裁剪（工件、卡片、门禁），但 CLAUDE.md 红线（来源标注、bear case、无终局判断、不编造数据）在**任何级别**都不裁剪。纪律是红线，流程是工具——工具可以选，红线不能松。
3. **升降级触发条件显式化**：
   - L0 → L1：初筛纪要里出现"值得进一步验证"的假设
   - L1 → L2：完成创始人首面，且假设清单中仍有 ≥ 2 条未验证的关键假设
   - L2 → L3：决定提交 IC，或单笔拟投金额超过阈值
   - 任何级别 → 终止：核心假设被证伪。终止时也要留一句话记录**为什么终止**（给设计十一的回填留素材）

### 15.3 与全文的关系

- 设计二（规格）：规格密度按级别取，L3 才启用 `spec/changes/`
- 设计三（Loop）：L2+ 可启用，人审检查点频率随级别加密
- 设计五（Compound）：L2+ 强制，L1 可选
- 设计十二（证据与主张图谱）：L3 完整五层，L2 Claim 从简（memo 内编号引用），L0/L1 只做来源标注

---

## 16. 全局架构：投资研究的 AI 研发成熟度模型

### 16.1 从线性阶梯到 2×2：混淆两个轴是原模型的缺陷

原书成熟度模型是 Level 1–5 的线性阶梯。直接迁移有个内在矛盾：bear case 强制、来源标注这些**纪律**，已被 CLAUDE.md 红线强制于"低自动化"阶段——它们不该出现在阶梯高处。问题出在线性模型混淆了**自动化程度**和**纪律强度**两个独立的轴。2×2 更诚实：

```
                    纪律强
                      │
        传统手工研究    │   目标区：强纪律 × 高自动化
        （严谨但慢，    │   ——纪律已编码进系统（反合理化、
        依赖个人修养）  │    验证门禁、规格驱动），
                      │    自动化放大的是可信度
   ───────────────────┼────────────────────
                      │
        裸奔 Vibe     │   危险区：自动化放大了
        Research      │   无纪律产出
        （快而随机）   │   ——批量生成"看起来不错"
                      │    的 memo，可信度塌陷
                      │
                    纪律弱
            自动化低 ───┼─── 自动化高
```

**当前位置**：自动化低 × 纪律中强（CLAUDE.md 红线 + `/jvc-*` 提供了纪律骨架，但执行靠人自觉，未编码进系统）。

**演进原则**：先把纪律编码进系统（反合理化表、验证门禁、研究规格），再提升自动化。反过来走——先堆自动化再补纪律——会直接坠入右下危险区：产出量爆炸，可信度塌陷。

### 16.2 自动化轴上的里程碑

原五级描述保留为自动化轴（横轴）上的里程碑，但不再暗示纪律自动随之提升：

- **M1 裸奔 Vibe Research**：跟 AI 聊天做研究，产出快、质量随机
- **M2 使用单个 Skill**：装了 `/jvc-*`，单个任务有保障，流程是散的
- **M3 Skill 串联成链路**：初筛→尽调→memo 有完整骨架，产出自动成为下游输入
- **M4 完整闭环**：Spec-Driven + 多角色对抗 + Compound 沉淀 + 验证门禁，从最终决策可追溯到每条证据的原始来源
- **M5 自主循环 + 持续改进**：监控类研究自动化运行，决策回填数据驱动方法论改进，多轨研究自动编排

### 16.3 当前状态评估

基于已有 `/jvc-*` 体系的现状：
- 自动化轴：位于 M2–M3 之间（有一组可独立工作的 Skill，部分设计了衔接关系）
- 纪律轴：中强（红线明确，但依赖人执行而非系统强制）
- 向目标区迈进的三个关键瓶颈：**纪律编码化**（反合理化进 SKILL.md、验证门禁进脚本）、**规格化**（Spec-Driven）、**判断校准基础设施**（决策日志——没有它，M5 的"数据驱动改进"没有数据源）

---

## 17. 落地路线图

每阶段配**可检查指标**——没有度量的方法论改进无法知道是否有效。

### 第一阶段：防弃用 + 打地基（本周可做）

1. **建立研究分级（L0–L3）**（设计十三）
   - 写入 CLAUDE.md 和各 SKILL.md 的适用级别声明
   - **指标**：新启动项目的档案恰好符合其级别要求，无过度工件
2. **统一权威目录结构**（§4.3）
   - 更新 README 项目档案约定 + 迁移说明
   - **指标**：一个真实项目按新结构归档完成
3. **为每个 `/jvc-*` Skill 补充反合理化表**（设计六）
   - **指标**：抽查 skill 输出——`[未核实]` 标注存在率、零终局判断语言
4. **CONTEXT.md 模板 + 假设清单格式**（设计一/二/十二）
   - 每条假设可证伪、带验证状态
   - 假设清单对齐 Claim schema（认识论四分：事实 / 公司口径 / 模型推算 / 待核查）

### 第二阶段：知识复利（本月可做）

5. **建立 STRATEGY.md**（设计五）
6. **`/jvc-compound` 强制沉淀**（L2+ 必做）
7. **建立决策日志**（设计十一）
   - 决策立案模板 + 回填提醒机制（先手工，每 6 个月）
   - **指标**：所有走完 L3 的项目（含不投的）100% 立案
8. **建立 tracks/ 知识库**：按赛道组织方法论文档、行业规律、数据源索引

### 第三阶段：验证与编排（下月可做）

9. **验证 lint 脚本**（设计十 12.3）
   - 来源标注、篇幅门禁、`[未核实]` 残留检查自动化
10. **历史回放 eval**（设计十第五层）
    - 选 2–3 个已知结局的历史项目回放研究流程
    - **指标**：盲区重现率——当初漏掉的风险，当前流程能抓出百分之多少
11. **虚拟投委会**（设计四）：核心四角色 + 私有信息注入 + 至少一个跨模型角色
12. **Research Loop 竞品监控**（设计三）：带人审检查点和预算护栏
13. **情景树结构化与 memo 回链**（设计十二）：Bull/Base/Bear 带条件·概率·触发器；lint 检查 IC memo 每条结论回链 Claim

### 第四阶段：全局编排（远期）

14. **Graph Research DAG 编排**（设计七）：锚点脚本校验、状态文件持久化
15. **研究 Harness**（设计八）：外发拦截 hook、权限模型、审计日志
16. **实体图谱与 SUPERSEDES 反馈回路**（设计十二）：跨项目 entities 库、Claim 版本链、memo diff 输出
17. **数据驱动的持续改进**：用决策回填数据分析哪类假设验证方法最有效，反馈到 Skill 改进
    - **指标**：次年 STRATEGY.md 修订引用的回填证据条数

---

## 附录 A：核心借鉴清单

| 序号 | 来源章节 | 核心概念 | 投资研究应用 |
|------|---------|---------|-------------|
| 1 | 卷首语 | 你可以外包思考，但不能外包理解 | 人可以委托 AI 收集信息、整理格式，但不能委托 AI 做判断 |
| 2 | 第 1 章 | Prompt-Driven → Skill-Driven | 已部分实现（`/jvc-*`），需深化反合理化设计 |
| 3 | 第 2 章 | 四个失败模式 → 四个修复 | 意图对齐、共享语言、垂直验证、架构审查——全部可映射 |
| 4 | 第 2 章 | CONTEXT.md 共享语言 | 项目级判断标准文档——高优先级 |
| 5 | 第 2 章 | `/handoff` 会话交接 | 研究会话边界管理——多阶段尽调的关键基础设施（复用 gstack 实现） |
| 6 | 第 3 章 | SDD：规格是人与 AI 的合约 | 研究规格驱动——"研究做完了"的定义前置 |
| 7 | 第 3 章 | 增量规格（Delta Specs） | 研究假设的增量更新——先有假设后有验证 |
| 8 | 第 4 章 | Ralph Loop：双出口门 | 自主循环研究——promise + max-iterations + 人审检查点 + 预算护栏 |
| 9 | 第 5 章 | 二十三专家角色 | 虚拟投委会——多视角对抗性审查（加私有信息注入防表演化） |
| 10 | 第 5 章 | 流程即代码（Process as Code） | 研究方法论编码为可版本控制的 Skill 文件 |
| 11 | 第 9 章 | 方法论对比与融合 | 不同研究阶段用不同方法论组合 |
| 12 | 第 10 章 | Harness Engineering 12 组件 | 研究 Agent 的安全运行环境（防外泄闸门在外发 hook，不在禁读） |
| 13 | 第 12 章 | Loop Engineering：五个原语 + 记忆 | 研究自动化循环的零件清单 |
| 14 | 第 12 章 | maker-checker 分离 | 生成分析 vs 验证分析——必须用不同的 Agent/会话 |
| 15 | 第 12 章 | 四条件测试 | "这个研究任务适合自动化吗？"的判定标准 |
| 16 | 第 13 章 | 上下文腐化及对抗策略 | 信息密集型研究的上下文管理（+ 文件腐化的慢性对策） |
| 17 | 第 13 章 | 瘦编排者 + 子 Agent 隔离 | 主会话只做编排，重活下放给隔离的子 Agent |
| 18 | 第 13 章 | STATE.md 持久化 | 项目研究状态的总控文件（状态翻转归脚本，不归 agent） |
| 19 | 第 14 章 | 强模型判断 + 弱模型执行 | 贵模型做研究设计、便宜模型做信息收集 |
| 20 | 第 14 章 | 计划即产品 | 研究计划和研究框架本身就是交付物 |
| 21 | 第 15 章 | Compound Engineering 复利循环 | 每次研究让下一次研究更容易（流程知识复利） |
| 22 | 第 15 章 | `/ce-compound` 强制沉淀 | 研究结束时的知识沉淀——从习惯变成强制环节（L2+） |
| 23 | 第 16 章 | 反合理化表 | 阻止 AI 在研究场景中跳过纪律 |
| 24 | 第 16 章 | 七阶段开发生命周期 | 研究全流程的阶段化纪律约束 |
| 25 | 第 17 章 | Graph Engineering：DAG 编排 | 多轨道并行研究的依赖管理 |
| 26 | 第 17 章 | 现实锚点 | 每个研究节点的完成由外部产物定义，不由 Agent 自我报告 |
| 27 | 后记 | 五条实战原则 + 四个常见陷阱 | 投资研究者的 AI 时代自我修养——待后记上线后补充 |

> **注**：设计零（类比的边界）、设计十一（决策日志）、设计十二（证据与主张图谱）、设计十三（研究分级）**不来自原书具体章节**——设计零/十一/十三处理的是投资研究相对于软件工程的*差异*；设计十二引入的是软件工程通识中的其他概念（增量构建、版本控制、双时态数据），并融入了《投资研究决策图》的四阶段数据架构。它们是迁移的补丁而非迁移本身。

---

## 附录 B：复用 vs 新建清单

本机已安装的工具与新设计之间的关系，避免重复造轮子：

| 能力 | 本机已有 | 决策 |
|------|---------|------|
| 会话交接 | gstack `/handoff`（已安装） | **复用**，包一层 jvc 语义（项目路径约定 + 脱敏规则）成为 `/jvc-handoff` |
| 目录锁定 / 权限边界 | gstack `/freeze`、`/guard`（已安装） | **直接复用**：freeze 到 `projects/<当前项目>/`，防止跨项目写入 |
| 需求盘问 | gstack `/office-hours`（六问结构） | **参考问法，新建** `/jvc-grill-me`——尽调语境的问题树不同（假设、证伪条件、竞品对标） |
| 决策日志 | `dbs-decision`（决策立案/结果回填/状态画像） | **复用其方法论结构**，落盘到项目目录 `decision-journal.md`（设计十一） |
| 多角色审查 | `/jvc-bear-case` 已有四角色 | **在其上扩展**虚拟投委会（私有信息注入 + 交叉质询），不新建框架 |
| 自主研究循环 | 无现成实现（书中有 Ralph Loop 概念） | **新建** `/jvc-research-loop`，带人审检查点与预算护栏 |
| 验证 lint | `scripts/` 已有 workbook/docx 校验脚本 | **扩展**：新增 memo 质量门禁脚本，复用现有 check-*.sh/py 的调用模式 |
| 历史回放 eval | `examples/`（golden 输出）+ `evals/trigger_cases.json` | **扩展**：从 skill 触发 eval 延伸到方法论回放 eval（设计十第五层） |
| 证据模型 / 研究分级 | 无 | **新建**（设计十二/十三），先以模板和约定落地，脚本化后置 |
| 实体图谱存储 | `/jvc-knowledge-tree-builder`（nodes.json / knowledge_graph.mmd / evidence_index.md） | **对齐复用**：Claim/实体沿用同一 JSON 约定，L3 启用跨项目 `tracks/{赛道}/entities/` 库 |

---

*本文档基于《AI时代的软件工程》（鸟窝，2026）全书的系统性阅读，将书中 17 章的方法论和原则迁移到投资研究工作流的设计中。v2 在迁移之外补充了四个"差异设计"（设计零/十一/十二/十三）——同构性决定能借鉴什么，差异性决定必须自建什么。v3 将《投资研究决策图》融入设计十二，把流程设计立在五层数据架构之上：流程是动词，图谱是名词——动词产生的东西，从此有了可累积的形态。*
